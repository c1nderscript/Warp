[package]
name = "repo_inventory"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "repo_inventory"
path = "repo_inventory.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.8"
chrono = { version = "0.4", features = ["serde"] }
